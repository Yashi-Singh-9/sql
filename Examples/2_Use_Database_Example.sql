-- USE company_db;
-- This command tells the database system to select the database
-- named "company_db" as the current working database.

-- After running this command, all SQL operations such as
-- creating tables, inserting data, updating records, and
-- querying data will be performed inside the "company_db" database.

-- Example:
-- If we create a table after this command, it will be created
-- inside the company_db database.

-- USE is required when the server has multiple databases
-- and we need to specify which database we want to work with.

USE company_db;